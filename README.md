# RJ-News
A lightweight, fast, and secure news application that fetches real-time headlines from NewsAPI. Built with vanilla HTML, CSS, JS and secured through a backend proxy using Vercel serverless functions (so the API key stays hidden).
